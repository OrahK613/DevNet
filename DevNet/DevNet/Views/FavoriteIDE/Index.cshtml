@model IEnumerable<DevNet.Models.FavoriteIDE>

@{
    ViewBag.Title = "Manage Favorite IDEs";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
    @Html.ActionLink("Manage Site", "Manage", "Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FavoriteIDEName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FavoriteIDEName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FavoriteIDEID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FavoriteIDEID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FavoriteIDEID })
        </td>
    </tr>
}

</table>
