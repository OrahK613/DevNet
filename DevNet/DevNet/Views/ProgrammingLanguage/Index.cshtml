@model IEnumerable<DevNet.Models.ProgrammingLanguage>

@{
    ViewBag.Title = "Manage Programming Languages";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create") |
    @Html.ActionLink("Manage Site", "Manage", "Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProgrammingLanguageName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProgrammingLanguageName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProgrammingLanguageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProgrammingLanguageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProgrammingLanguageID })
        </td>
    </tr>
}

</table>
