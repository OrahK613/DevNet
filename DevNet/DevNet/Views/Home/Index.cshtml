@using DevNet.Models;
@model DevNet.Models.RSSFeedModel

@{
   string IsConfirmed = "false"; 
   
   if(Model.IsConfirmed != null)
   {
       IsConfirmed = Model.IsConfirmed;
   }
  

}

<style>

    .nobullets
    {
        list-style-type: none;
    }
    .darkscreen
    {
         color: #999;
         padding: 25px;
         background-color: rgba(21,36,57,.5);
         /*border-radius: 30px;*/
         border: 2px solid rgba(31, 46, 67, 1);
         box-shadow: 5px 0px 20px #000000 inset, 0px 0px 20px rgba(21, 36, 57, 0.6);
         position: relative;
         /*top: 25px;
         left: 105px;*/
         height: 300px;
         width: 400px;
         z-index: 200;
         left: 80px;
         float: right;
    }

    .float
    {
       
        width: 400px;
        height: 1000px;
 
        color: #999;
         padding: 25px;
         background-color: rgba(0,0,0,.5);
         /*border-radius: 30px;*/
         border: 2px solid rgba(31, 46, 67, 1);
         box-shadow: 5px 0px 20px #000000 inset, 0px 0px 20px rgba(21, 36, 57, 0.6);
         position: relative;
         z-index: 200;
         /*top: -200px;*/
         /*left: -300px;*/
         font-size: 10pt;

    }
   

</style>

<div class="jumbotron">

    <div class="darkscreen">

        <p class="lead">DevNet is a portal that serves as an online community for developers and IT employeers to network and talk about technology as well as share knowledge.</p>

        <ul style="list-style-type: none;"><li style="list-style-type: none;">@Html.ActionLink("Learn more", "About", "Home", new { @class = "btn btn-primary btn-lg nobullets" })&raquo;</li></ul>

        @*<a href="javascript:confirmRSS()">Confirm RSS</a>*@

    </div>
    @*@if (IsConfirmed == "true")
    {
        Html.RenderPartial("_RSSFeedPartial"); 
    }*@
    @{Html.RenderPartial("_RSSFeedPartial"); }

    @*@if (IsConfirmed == "true")
    {
        <div class="row float">

            <h2>@Model.RssFeedName RSS Feed</h2>
            <ul>
                @foreach (System.ServiceModel.Syndication.SyndicationItem feed in (List<System.ServiceModel.Syndication.SyndicationItem>)Model.RssFeed)
                {
                    <li><a href="@feed.Id" target="_blank">@feed.Title.Text </a></li>
                }
            </ul>


        </div>
    }*@
</div>




    
@*@section topLevel{

    @{ Html.RenderPartial("_RenderRSSConfirmDialog"); }

}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @{ Html.RenderPartial("_ConfirmRSSFeed.js"); }
}

<script>


    window.onload = function ()
    {

        //You can call it like
        runFunction('@Model.CallFunction', ['runFunction is being called']); //alerts test.
    };

    function runFunction(name, arguments) {

        var fn = window[name];

        var type = typeof fn;


        if (typeof fn !== 'function') {
            return;
        }
        else {
            fn.apply(window);
        }


        // fn.apply(window, arguments);

        }

</script>*@