@model IEnumerable<DevNet.Models.SoftwareSpecialty>

@{
    ViewBag.Title = "Manage Software Specialties";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create") |
    @Html.ActionLink("Manage Site", "Manage", "Home")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoftwareSpecialtyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SoftwareSpecialtyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SoftwareSpecialtyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SoftwareSpecialtyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SoftwareSpecialtyID })
        </td>
    </tr>
}

</table>
